name: ai_avatar_demo
description: "AI Avatar voice interaction demo using ZEGO Express SDK"
publish_to: 'none'
version: 1.3.0+3

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  zego_express_engine: ^3.10.3
  http: ^1.2.0
  flutter_dotenv: ^5.2.1
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
